<html>
<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <title>Pot Rud Uni</title>
</head>
<body>
    <div class="container">
        <h1>Here is your list of courses</h1>

        {% if courses is not empty %}
            <p>Courses available at Pot Rud Uni:</p>
            <ul>
                {% for course in courses %}
                    <li> {{ course.getCourseName }}, {{ course.getNumber }} </li>
                {% endfor %}
            </ul>
        {% else %}
            <p>There are no courses!</p>
        {% endif %}
        <br>
        <h4>Add a new course:</h4>
        <form action='/courses' method="post">
            <label for='course_name'>Course Name:</label>
            <input type="text" name='course_name' id='course_name'>
            <label for='number'>Course Number:</label>
            <input type="text" name='number' id='number'>
            <button type="submit" class="btn btn-success">Add them!</button>
        </form>

        <form action="/delete_courses" method="post">
            <button type="submit" class="btn btn-danger">Burn them!</button>
        </form>

        <h3><a href="/">Home</a></h3>

    </div>

</body>
</html>
